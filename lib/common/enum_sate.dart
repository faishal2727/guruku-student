enum RequestStateLogin { initial, empty, loading, loaded, error }

enum RequestStateRegister { initial, empty, loading, loaded, error }

enum RequestStateRefreshOtp { initial, empty, loading, loaded, error }

enum RequestStateReqForgotPw { initial, empty, loading, loaded, error }

enum RequestStateForgotPw { initial, empty, loading, loaded, error }

enum RequestStateVerifyOtp { initial, empty, loading, loaded, error }

enum RequestStatePayment { initial, empty, loading, loaded, error }

enum RequestStateOrder { initial, empty, loading, loaded, error }

enum RequestState { initial, empty, loading, loaded, error }

enum RequestStateReview { initial, empty, loading, loaded, error }

enum RequestStateDetail { initial, empty, loading, loaded, error }

enum ReqStateRegisTeacher { initial, empty, loading, loaded, error }

enum ReqStateTeacher { initial, empty, loading, loaded, error }

enum ReqStateSchedule { initial, empty, loading, loaded, error }

enum ReqStateProfile { initial, empty, loading, loaded, error }

enum ReqStateAvatar { initial, empty, loading, loaded, error }

enum ReqOdrTeacPen { initial, empty, loading, loaded, error }

enum ReqOdrTeacSuc { initial, empty, loading, loaded, error }

enum ReqOdrTeacCan { initial, empty, loading, loaded, error }

enum ReqOdrTeacDone { initial, empty, loading, loaded, error }

enum ReqOdrTeacDetail { initial, empty, loading, loaded, error }

enum ReqPresent { initial, empty, loading, loaded, error }

enum ReqTidak { initial, empty, loading, loaded, error }

enum ReqStateNotif { initial, empty, loading, loaded, error }

